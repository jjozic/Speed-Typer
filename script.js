var words = ['hello', 'how', 'house', 'boy', 'girl', 'planet']
var randomWord = words[Math.floor(Math.random()*words.length)]
var run = true
while(run){
    //setTimeout(console.log(randomWord), 500)
}